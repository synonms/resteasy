@using Radzen.Blazor

<RadzenButton Icon="add_circle_outline" Text="Add" Click="@(() => NavigationManager.NavigateTo(Href))"></RadzenButton>

@code {
    [Inject]
    public NavigationManager NavigationManager { get; set; } = null!;

    [Parameter] 
    [EditorRequired] 
    public string Href { get; set; } = null!;
}